# Production Requirements
# Use: pip install -r requirements-prod.txt

# ===== WSGI Server (Required for Production) =====
gunicorn==21.2.0

# ===== Core Flask Dependencies =====
# Included from requirements.txt
-r requirements.txt

# ===== Environment Configuration =====
python-dotenv==1.0.0

# ===== Production Database Support =====
psycopg2-binary==2.9.7  # PostgreSQL adapter

# ===== Security Enhancements =====
Flask-Talisman==1.1.0   # Security headers (HTTPS, CSP, etc.)

# ===== Error Tracking & Monitoring =====
sentry-sdk[flask]==1.28.1  # Error reporting

# ===== Development Tools (for production debugging) =====
Flask-Migrate==4.0.5    # Database migrations

# ===== Production Web Server Setup Notes =====
# 1. Use Gunicorn: gunicorn -w 4 -b 0.0.0.0:8000 wsgi:app
# 2. For Vercel, Heroku, Railway, etc.: They auto-detect and use wsgi:app
# 3. Consider using a reverse proxy (nginx) in front of Gunicorn
# 4. Always use HTTPS/SSL in production
# 5. Never commit .env files with real secrets - use environment variables</content>
<parameter name="filePath">c:\Users\PC\OneDrive - United States International University (USIU)\Desktop\USIU-NOTES\USIU-NOTES 2026\APT4900\secured site\secured site\requirements-prod.txt